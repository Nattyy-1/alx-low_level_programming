#inlcude "main.h"

/**
 * add - adds the given two numbers
 * @n: the first digit to be added
 * @m: the second digit to be aded
 *
 * Return: the sum of two digits
 */
int add(int n, int m)
{
	return (n + m);
}
