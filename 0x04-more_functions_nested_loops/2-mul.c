#include "main.h"

/**
 * mul - multplies two integers
 * @a: the number being multiplied (multiplicand)
 * @b: the number we multiply by (multiplier)
 *
 * Return: the product of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
