#include "main.h"

/**
 * _abs - used to get the absoulute value of a given number
 * @n: the number to find absoulute value for
 *
 * Return: absoulte value of the number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

